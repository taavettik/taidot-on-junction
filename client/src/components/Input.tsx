import styled from 'styled-components';

export const Input = styled.input`
  border: 1px solid ${(p) => p.theme.colors.mutedDarkNeutral};
  padding: 24px;
  border-radius: 16px;
  font-size: 16px;
`;
